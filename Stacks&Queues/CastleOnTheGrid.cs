using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;

namespace CastleOnTheGrid
{
    public class CastleOnTheGrid
    {
        public static void MinimumMoves(string[] grid, int startX, int startY, int goalX, int goalY)
        {
            // string total = string.Empty;
            // Stack<int> stk = new Stack<int>();
            // int minPath = 0;
            // for(int i=0; i<grid.Length; i++)
            // {
            //     total += grid[i];
            // }
            // char[]chArr = total.ToArray();

            // if( goalX > startX)
            // {
            //     //Move Right
            //     int x = startX;
            //     int y = startY;
            //     while( x != goalX && x <  )
            // }

        }
    }
}